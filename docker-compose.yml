version: '3.8'
services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - VECTOR_INDEX_PATH=/app/vector_index
    volumes:
      - ./data:/app/data
      - ./vector_index:/app/vector_index
